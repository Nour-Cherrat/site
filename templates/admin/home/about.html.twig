{% extends'base.html.twig' %}

{%  block title 'Editer la presentation' %}

{% block body %}

    <div class="container mt-4" style="width: 60%">
        <div class="jumbotron text-center">
            <h1>Editer la presentation</h1>
        </div>
        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button class="btn btn-primary">Editer</button>
        {{ form_end(form) }}
    </div>




{% endblock %}