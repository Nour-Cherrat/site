{% extends'base.html.twig' %}

{%  block title 'Gestion Home' %}

{% block body %}

    <div class="container mt-4">
        <div class="jumbotron text-center">
            <h1>Gérer Home</h1>
        </div>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

        <table class="table table-striped">
            <thead>
            <tr>
                <th>presentation</th>
                <th>mission</th>
                <th>vision</th>
                <th>histoire</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            {% for pres in present %}
                <tr>
                    <td>{{ pres.about }}</td>
                    <td>{{ pres.mission }}</td>
                    <td>{{ pres.vision }}</td>
                    <td>{{ pres.histoire }}</td>
                    <td>
                        <a href="{{ path('admin.home.about', {id: pres.id}) }}" class="btn btn-secondary">Editer</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>



{% endblock %}